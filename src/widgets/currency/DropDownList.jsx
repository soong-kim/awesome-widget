import React from 'react';

function DropDownList(props) {
/*
  const selectList = (
    <select name="CURRENCY_LIST" id="CURRENCY_LIST" onchange="props.onSelect())"></select>
  );

  const optionList = props.optionList;
  const element = document.getElementById("CURRENCY_LIST");
  for (let i = 0; i < optionList.length; i++) {
    const option = new Option(optionList[i].text, optionList[i].value);
    element.appendChild(option);
  }

  return selectList;
*/
  return <span>"CURRENCY_LIST"</span>
}

export default DropDownList;
